import { VisionSortPipe } from './vision-sort.pipe';

describe('VisionSortPipe', () => {
  it('create an instance', () => {
    const pipe = new VisionSortPipe();
    expect(pipe).toBeTruthy();
  });
});
